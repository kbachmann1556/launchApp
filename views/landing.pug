extends layout

block content
	.container(style='text-align: center')
		.page-header
			h1 Launch App landing page
		.row
			form
				input(type="text", placeholder="username")
				input(type="password", placeholde="password")